# Core — keep lean for internal/portable use
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
pandas>=2.0.0
openpyxl>=3.1.0
python-dotenv>=1.0.0
rich>=13.0.0
pydantic>=2.0.0

# AI — Claude (Ollama needs no pip package; uses httpx)
anthropic>=0.40.0

# Optional browser path (JS pages / login):
#   pip install playwright && playwright install chromium
# playwright>=1.40.0
